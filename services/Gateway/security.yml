# JWT validation at the gateway edge (must match users-service issuer/audience/secret).
Jwt:
  Issuer: "Tangle"
  Audience: "TangleClient"
  Secret: "CHANGE_THIS_TO_A_LONG_RANDOM_SECRET_KEY_32+_CHARS"
