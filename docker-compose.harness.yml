# Harness runs tests inside Compose; API is reached at http://api:8080 (no host port required).
services:
  api:
    ports: !reset []
    environment:
      # SAS URLs must target azurite:10000 in-network; rewriting the host breaks the signature.
      Media__PublicBlobEndpoint: ""
